import React from 'react';
import { View } from 'react-native';

/**
 * Componente vazio - Sistema de notificações ativo em background
 * Será substituído por interface melhor na branch principal
 */
export const NotificationTestPanel: React.FC = () => {
  return <View />;
};
